[project]
name = "mcp-python-starter"
version = "0.1.0"
description = "A minimal Model Context Protocol (MCP) server in Python."
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "mcp>=1.9.4",
    "requests>=2.31.0",
    "flask>=3.0.0"
]
